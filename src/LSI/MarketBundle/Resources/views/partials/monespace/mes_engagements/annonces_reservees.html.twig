
<div class="row card" style="margin-bottom: 15px;">
    <div class="card-header">
        <h4 class="card-title">Liste de mes annonces reservées</h4>
    </div>
    <div class="card-body">
        <table id="annonces-reservees-table" class="table table-striped table-bordered" style="width:100%;">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Objets</th>
                    <th>Date début</th>
                    <th>Date fin</th>
                    <th>Dépensé</th>
                </tr>
            </thead>
            <tbody>
                {% for index , reservation in annoncesReservees %}
                <tr>
                    <td>{{ index + 1 }}</td>
                    <td>{{ reservation.annonce.titre }}</td>
                    <td>{{ reservation.debutPrestation | date("Y-m-d")}}</td>
                    <td>{{ reservation.finPrestation | date("Y-m-d")}}</td>
                    <td>{{ reservation.montantReservation }} </td>
                </tr>
                {% endfor %}
            </tbody>
        </table> 
    </div> 
</div>
